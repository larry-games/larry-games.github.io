# TODO

- copy images from old web to new web /img
- keep folder structure the same

`npm install gh-pages --save-dev`
`git remote add origin https://github.com/re-mark-able/larrygames.git`
`npm run deploy`

or do it via cloudflare?

- larrygames.github is taken.

fixes
test
